---
# For deploy-only runs (--tags foray:deploy), pass the droplet IP via:
#   -e foray_droplet_ip=<ip>
# The playbook will fail if foray_droplet_ip is not set.
all:
  children:
    foray:
      hosts:
        foray-prod:
          ansible_host: "{{ foray_droplet_ip }}"
          ansible_user: root
