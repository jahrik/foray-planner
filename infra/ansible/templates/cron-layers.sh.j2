#!/bin/sh
# Managed by the foray-planner ansible role
exec flock -n /tmp/foray-layers.lock \
  docker run --rm --env-file /opt/foray-planner/foray.env \
    {{ foray_app_image }} \
    foray refresh --with camps,land,dispersed,trails
