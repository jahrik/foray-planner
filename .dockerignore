# VCS / local tooling
.git
.gitignore
.github

# Python caches / build artifacts
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv

# Local cache + personal runtime state (rebuilt in-container on the volume)
data/*.duckdb
data/*.duckdb.wal
data/location.json

# Node deps + generated schema input — the frontend stage runs `npm ci` and builds fresh
node_modules
frontend/openapi.json

# Local-only notes and docs not needed in the image
TODO.md
