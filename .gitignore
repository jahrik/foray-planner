# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Node deps + built client bundle (rebuilt via `cd frontend && npm run build`)
node_modules/
src/foray/web/dist/

# Local scratch dir: iNaturalist cache (rebuildable via `foray refresh`), runtime location
# override (personal state, set from the UI), and one-off data-pipeline scripts/downloads -
# never meant to be committed.
data/

# Local secrets (e.g. RIDB_API_KEY) - never commit; use env vars in prod
.env

# Personal planning notes (local only)
TODO.md

# playwright-mcp session output (snapshots/console logs from manual browser testing)
.playwright-mcp/

# Ansible local collection/role/module cache (reinstalled via `make ansible-install`)
infra/ansible/.ansible/
