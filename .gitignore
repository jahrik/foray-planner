# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Node deps + built client bundle (rebuilt via `cd frontend && npm run build`)
node_modules/
src/foray/web/dist/

# Local iNaturalist cache (rebuildable via `foray refresh`)
data/*.duckdb
data/*.duckdb.wal

# Runtime location override (personal state, set from the UI)
data/location.json

# Local secrets (e.g. RIDB_API_KEY) - never commit; use env vars in prod
.env

# Personal planning notes (local only)
TODO.md
